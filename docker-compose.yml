services:
  postgres:
    image: postgis/postgis:17-3.5
    container_name: papsampah-postgis
    restart: unless-stopped
    environment:
      POSTGRES_DB: papsampah
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
